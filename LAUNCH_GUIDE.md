dd# Балаган — Инструкция по запуску

## Первый запуск

1. Открой **Godot 4.3+** (или 4.6)
2. Нажми **Import** → укажи папку проекта `Godot game`
3. Godot найдёт `project.godot` и импортирует проект
4. Нажми **Edit** чтобы открыть редактор

## Запуск игры

- Нажми **F5** или кнопку Play (треугольник) вверху справа
- Игра запустится в окне 1920x1080

## Управление

| Клавиша | Действие |
|---------|----------|
| A / стрелка влево | Идти влево |
| D / стрелка вправо | Идти вправо |
| Shift | Бежать |A
| E | Взаимодействие / Зарядить фонарик |
| Q | Спрятаться (рядом с укрытием) |
| Tab или I | Инвентарь |
| Пробел / Enter | Пропустить текст / Следующая реплика |

## Если есть ошибки при запуске

### "Cannot find animation" или подобные
Это значит что AnimatedSprite2D не имеет нужных анимаций. Плейсхолдерные SpriteFrames уже добавлены — если ошибка осталась, перезапусти Godot.

### Чёрный экран
Проверь что `run/main_scene` в project.godot указывает на `res://scenes/rooms/room_main_hall.tscn`.

### Персонаж невидим
Плейсхолдер-спрайты — цветные прямоугольники. Если не видно, проверь что `player_frames.tres` загружается (в AnimatedSprite2D → SpriteFrames).

## Структура плейсхолдеров

- Синий прямоугольник 32x64 = игрок
- Бежевый прямоугольник 24x20 = лайка
- Тёмно-фиолетовый 32x48 = дух-хранитель
- Серые 20x48 = двери
- Жёлтый 16x16 = предмет
- Коричневый 32x48 = укрытие

## Замена спрайтов на свои

1. Положи свой PNG в `assets/sprites/`
2. Открой соответствующий `.tres` файл в `assets/sprites/`
3. Замени путь к текстуре на свой файл
4. Или в редакторе: выбери AnimatedSprite2D → SpriteFrames → добавь кадры
