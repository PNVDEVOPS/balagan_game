.godot/
*.import
export_presets.cfg
movie.avi

# Editor / tooling noise (regenerable, machine-local)
.history/
graphify-out/
.claude/settings.local.json

# Build output (exported .exe/.pck — large, regenerable)
build/
